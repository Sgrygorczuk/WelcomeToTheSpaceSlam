using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Numbers : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        print(100);
        print(-5);
        print(2.5f);
        print(-2.4f);
        print(10*10-40+2+102);
        print(10*5%2);
        print((5 - 3) * 10 - (3 * 2 - 2));
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
